[user]
	name = Matt St. Clair
	email = matt.st.clair@plexus.com
[core]
[push]
	default = simple
[color]
	ui = auto
[merge]
[core]
	excludesfile = ~/.gitignore
[merge]
	tool = meld
[pager]
	branch = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
